/* v29 — RETIRED.
   This suite covered the hex board's card-question mode, which was removed when
   Outpost became a real strategy game (no quiz anywhere). Its replacement is
   _test_v31, which covers the board, the resource economy, buildings, reach and
   the offline production cap. Kept as a stub so the run list stays honest. */
console.log("— v29 retired: the hex quiz game no longer exists. See _test_v31.");
console.log("✅ 0 passed, 0 failed");
